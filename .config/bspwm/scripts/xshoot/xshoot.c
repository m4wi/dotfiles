#include <stdio.h>
#include <string.h>

int main (int argc, char **argv) {
  char *option = argv[1];
  switch (option) {
  case '--now':
    break;
  case '--area':
    break;
  case '--root':
    break;
  default:
    break;
  }
  return 0;
}